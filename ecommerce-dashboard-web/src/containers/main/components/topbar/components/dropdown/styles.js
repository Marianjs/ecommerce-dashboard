import styled from 'styled-components';

export const DropdownContainer = styled.li`

    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: #787878;
    padding: 0.5rem;
    word-break: break-word;

`;